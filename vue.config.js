module.exports = {
    publicPath: process.env.NODE_ENV === 'production'
        ? '/Viva_Mebel_project/'   // 🔥 осында сенің repo атауың болу керек!
        : '/'
}
